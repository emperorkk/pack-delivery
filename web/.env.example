# Cloudflare Worker that proxies Soft1 (required — Soft1 emits no CORS)
VITE_SOFT1_PROXY=https://pack-delivery-proxy.example.workers.dev

# Public URL where the CST installer file is hosted (shown in the "CST missing" dialog)
VITE_CST_DOWNLOAD_URL=https://pack-delivery.pages.dev/aicmp.pack-delivery.CST
